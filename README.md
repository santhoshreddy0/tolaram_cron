# tolaram_cron

- install pip

- install virtual env

  ```
  pip install virtualenv
  ```
- create venv

  ```
  python3 -m venv venv   
  ```          

- activate virtual env

  ```
    source venv/bin/activate
    ```

- install packages
  ```
    pip install -r requirements.txt
  ```
